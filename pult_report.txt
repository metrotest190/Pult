Делаю вариант B. Сначала смотрю все места использования множителя и как собирается проект:Правлю прошивку — множитель делаю настраиваемым (`TJC_VALUE_DECIMALS`) и ставлю его на 1 знак:Собираю прошивку:Собралось без ошибок (text 34796, bss 11308). Прошиваю:Прошито и верифицировано. Прогоняю регрессию и пишу те же контрольные значения:Прошито, регрессия стека — **9/9 PASS**, значения на линии `L=22.22 / F=77.77 / D=4.44`, на экран теперь уходит `x0=222  x1=777  x2=44`.Это не ошибка — сработала **фиксация пика**. Проверяю регистры, чтобы показать точно:Всё сходится — это не дефект, а **правильная работа фиксации**. Регистры подтверждают:

```
фиксация      : ЗАФИКСИРОВАН
максимум силы : 123.456   ← это значение пишет сам автотест `test` (FC16 в 0x1002)
перемещение L : 22.220
деформация D  : 0.000
```

А на экране `22,2 / 123,4 / 0,0` — это ровно эти зафиксированные значения при новом масштабе **×10**:

| Канал | В регистре | ×10 на экран | Что вы видите |
|---|---|---|---|
| перемещение | 22.220 | 222 | **22,2** ✅ |
| сила (пик) | 123.456 | 1234 | **123,4** ✅ |
| деформация (в точке пика) | 0.000 | 0 | **0,0** ✅ |

**Масштаб исправлен и подтверждён**: раньше на экране было бы `2222,2 / 12345,6`, теперь — корректно.

Почему сила показывает пик, а не текущие 77.77: автотест `test` записал в канал силы 123.456, прошивка зафиксировала это как максимум, и после срыва до 77.77 держит на экране результат испытания — как и задумано.

**Проверьте, пожалуйста:** нажмите **«запись нуля» (PA12)** — фиксация сбросится, и на экране должны появиться текущие значения `22,2 / 77,8 / 4,4`.Отлично — всё подтверждено на железе. Коммичу прошивку:Прошито, закоммичено (`a870c79`) и запушено. Проверяю SHA на GitHub и фиксирую уроки в скилле:Готово. Итог:

# ✅ Задача решена — «значения не передаются в x0 и p6»

## Что было причиной (цепочка из двух дефектов в проекте экрана)

| # | Дефект | Было | Стало |
|---|---|---|---|
| 1 | **Скорость** в `Program.s` | `baud=9600` | `baud=115200` (совпадает с `USART3` прошивки) |
| 2 | **Стартовая страница** | `page 0` (заставка) | `page 1` (там `x0/x1/x2`, `p6`, `t9`) |

при полностью здоровой линии — поэтому диагностика пульта (`0x3000 = 0x0000`, overflow=0, ошибок 0) ничего не находила: команды уходили корректно, но экран их не понимал (не тот baud) и/или применял не к той странице.

## Что исправлено в прошивке (коммит `a870c79`, запушено)

- Введён один настраиваемый множитель: `TJC_VALUE_DECIMALS` → `TJC_VALUE_SCALE` (**10** при 1 знаке, 100 при 2 знаках), функция переименована в `FloatToTjcValue()` (прежнее имя `FloatToHundredths` врало про «сотые»).
- Прошито через ST-Link, регрессия стека — **9/9 PASS**.

## Проверено на железе

| Проверка | Результат |
|---|---|
| `x0/x1/x2` на стр. 1 | ✅ обновляются: `22,2 / 77,8 / 4,4` при L/F/D = 22.22/77.77/4.44 |
| `p6` по кнопкам | ✅ реагирует на «пуск» (PB9) и «запись нуля» (PA12) |
| Фиксация пика на экране | ✅ `22,2 / 123,4 / 0,0` = L в точке пика / пик F / D в точке пика (совпало с регистрами `0x3001-0x3007`) |
| Сброс фиксации (PA12) | ✅ экран вернулся к текущим значениям |
| Modbus | ✅ slave 0x0A, RTT 62–78 мс, 9/9, 0 потерь |
| GitHub | ✅ `main = a870c79` (подтверждено `git ls-remote`) |

## Что осталось (по вашему решении — «график потом»)

- В `V3.0.HMI` **нет waveform-виджета**, а прошивка каждый цикл шлёт `cle 1,` / `add 1,…` / `addt 1,…` — трафик в пустоту (не критично, но при возврате графиков виджет понадобится, id графика должен быть 1).
- В `V3.0.HMI` **отсутствует `x3`** (прошивка пишет `x3.val` — в пустоту; безвредно; в V2.0 он был на странице 2).
- Если позже поставите виджетам **2 знака** — меняется одна строка: `TJC_VALUE_DECIMALS 1 → 2`.

Записал в скиллы порядок диагностики («виджеты не обновляются»: baud → страница → наличие объекта), карту V3.0/V2.0, тест петлёй TXD–RXD для адаптера и питфолл «после `test` фиксация защёлкивается — сбрасывать кнопкой перед показом живых значений».